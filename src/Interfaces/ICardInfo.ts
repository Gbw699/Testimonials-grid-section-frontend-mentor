export interface ICardInfo {
  profileImg: string;
  name: string;
  shortTestimony: string;
  largeTestimony: string;
  color: Color;
}

interface Color {
  color: string;
  backgroundColor: string;
}
